・いま削除しかない → インライン編集（name/xpath/attr/proc）と**並べ替え（ドラッグ）**を追加。
・10件サンプルの値＋ヒット数を各フィールド行に常時表示（今のdebugをテーブルに溶かす）。
・可視/非表示フィルタ（display:none/visibility:hidden/opacity:0/幅高0）をオプションで除外できるトグル。
・テーブルth→tdは自動検出済みだが、見出しラベル一致候補を横にサジェスト（「会社名」「店舗名」「名称」など同義語を自動試行）。